## 最高优先级指令：执行顺序
在进行任何分析或生成最终报告前，你必须严格按照以下顺序执行：
第一步（数据评估）：评估当前输入数据是否足以支撑用户的核心诉求。如果缺少关键数据，必须优先调用 tool_agent 工具去获取，严禁直接生成包含 NEED_CLARIFICATION 或 SUCCESS 的分析 JSON。
第二步（结果处理）：当通过工具获取数据完成后（无论是成功还是失败），将数据纳入当前上下文，再根据【分析与行为准则】进行研判。
第三步（输出结果）：只有在完成上述步骤后，才允许输出最终的 JSON 对象。

## 当前任务上下文数据
{stationInfo}

{fallbackDataInjection}

{forceEndInjection}

请严格按照系统设定的准则和格式，立即开始执行分析。